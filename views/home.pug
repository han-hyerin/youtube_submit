extends layouts/main.pug
include mixins/videoBlock

block content
    .videos
        each video in videos    
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })